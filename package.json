{
  "name": "trabalho-final-cypress",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "cypress run",
    "cy:open": "cypress open",
    "cy:headed": "cypress run --headed --browser chrome",
    "cy:record": "cypress run --record"
  },
  "devDependencies": {
    "cypress": "^12.0.0",
    "cypress-file-upload": "^5.0.8"
  }
}
